const initialState = {
  
};

const MovieSeat = (state = initialState, action) => {
  switch (action.type) {
    case "CHOSE":
      {
        
      }
      break;
    default:
      return { ...state };
  }
};

export default MovieSeat;
